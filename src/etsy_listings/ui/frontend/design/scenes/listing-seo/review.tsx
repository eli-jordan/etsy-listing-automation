import { ListingSeoReviewScreen } from "../../screens/ListingSeoReviewScreen";

export const meta = {
  title: "Listing SEO — AI Mode prototype",
  viewport: "laptop",
  description: "Single clickable prototype: AI Mode loads in place, then reveals title, lead, and ranked tag suggestions automatically.",
};

export default function Review() {
  return <ListingSeoReviewScreen />;
}
