// Retired. The real copy now lives at design/scenes/archive/batch-deploy-result.tsx and is
// pinned on the archive board; this file is a leftover pointer because the session that
// retired it had no shell to delete it. Safe to delete: nothing links here.
import BatchResultWireframe from "../archive/batch-deploy-result";

export const meta = {
  title: "Batch deployment — result (retired) · v1",
  viewport: "laptop",
  description:
    "Retired: moved to archive/batch-deploy-result. This leftover file is safe to delete.",
};

export default function RetiredBatchResult() {
  return <BatchResultWireframe />;
}
