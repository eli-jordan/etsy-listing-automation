"""Backs the ``new`` CLI command. Named ``newcmd`` rather than ``new`` --
purely to avoid a package that shadows the common local variable name ``new``
everywhere else in the codebase; the CLI command itself is still ``new``."""
